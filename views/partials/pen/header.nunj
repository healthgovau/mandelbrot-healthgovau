<div class="row">

    <div class="col-xs-12">
        <h1 class="Pen-title">
            {{ entity.title }}
        </h1>

        {% if entity.notes %}
        {{ frctl.docs.renderString(entity.notes, renderEnv) | async }}
        {% endif %}
    </div>

</div>